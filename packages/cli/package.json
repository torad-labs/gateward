{
  "name": "portable-hooks",
  "version": "0.1.0",
  "description": "Installs portable ast-grep hooks (rule packs + engine) into a project and wires them into detected AI coding harnesses (Claude Code, Codex, OpenCode).",
  "bin": {
    "portable-hooks": "./dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "test": "tsc && chmod +x dist/index.js && cd dist && node --test"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3"
  }
}
