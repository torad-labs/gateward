{
  "name": "gateward",
  "version": "0.1.0",
  "description": "The keeper of the write gate: installs ast-grep rule packs + the gate engine into a project and wires them into AI coding harnesses (Claude Code, GitHub Copilot, Codex, OpenCode).",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/torad-labs/gateward.git",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/torad-labs/gateward",
  "bugs": "https://github.com/torad-labs/gateward/issues",
  "bin": {
    "gateward": "./src/index.ts"
  },
  "engines": {
    "bun": ">=1.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "valibot": "^1.4.2"
  }
}
